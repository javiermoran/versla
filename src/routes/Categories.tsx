import React, { ReactElement } from 'react';
import VerslaContainer from '../components/VerslaContainer';

const Categories = (): ReactElement => {
  return <VerslaContainer title='Categorías'></VerslaContainer>;
};

export default Categories;
